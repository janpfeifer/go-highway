// Code generated by hwygen. DO NOT EDIT.

package matmul

import (
	"github.com/ajroetker/go-highway/hwy"
)

func BaseBlockMulAdd_fallback(aT []float32, b []float32, c []float32, blockDim int) {
	if len(aT) < blockDim*blockDim {
		panic("BlockMulAdd: aT slice too short")
	}
	if len(b) < blockDim*blockDim {
		panic("BlockMulAdd: B slice too short")
	}
	if len(c) < blockDim*blockDim {
		panic("BlockMulAdd: C slice too short")
	}
	lanes := hwy.Zero[float32]().NumLanes()
	for i := 0; i < blockDim; i++ {
		cRowStart := i * blockDim
		for k := 0; k < blockDim; k++ {
			aik := aT[k*blockDim+i]
			vA := hwy.Set(aik)
			bRowStart := k * blockDim
			var j int
			for j = 0; j+lanes <= blockDim; j += lanes {
				vB := hwy.Load(b[bRowStart+j:])
				vC := hwy.Load(c[cRowStart+j:])
				vC = hwy.MulAdd(vA, vB, vC)
				hwy.Store(vC, c[cRowStart+j:])
			}
			for ; j < blockDim; j++ {
				c[cRowStart+j] += aik * b[bRowStart+j]
			}
		}
	}
}

func BaseBlockMulAdd_fallback_Float64(aT []float64, b []float64, c []float64, blockDim int) {
	if len(aT) < blockDim*blockDim {
		panic("BlockMulAdd: aT slice too short")
	}
	if len(b) < blockDim*blockDim {
		panic("BlockMulAdd: B slice too short")
	}
	if len(c) < blockDim*blockDim {
		panic("BlockMulAdd: C slice too short")
	}
	lanes := hwy.Zero[float64]().NumLanes()
	for i := 0; i < blockDim; i++ {
		cRowStart := i * blockDim
		for k := 0; k < blockDim; k++ {
			aik := aT[k*blockDim+i]
			vA := hwy.Set(aik)
			bRowStart := k * blockDim
			var j int
			for j = 0; j+lanes <= blockDim; j += lanes {
				vB := hwy.Load(b[bRowStart+j:])
				vC := hwy.Load(c[cRowStart+j:])
				vC = hwy.MulAdd(vA, vB, vC)
				hwy.Store(vC, c[cRowStart+j:])
			}
			for ; j < blockDim; j++ {
				c[cRowStart+j] += aik * b[bRowStart+j]
			}
		}
	}
}

func BaseBlockMulAdd2_fallback(aT []float32, b []float32, c []float32, blockDim int) {
	if len(aT) < blockDim*blockDim {
		panic("BlockMulAdd2: aT slice too short")
	}
	if len(b) < blockDim*blockDim {
		panic("BlockMulAdd2: B slice too short")
	}
	if len(c) < blockDim*blockDim {
		panic("BlockMulAdd2: C slice too short")
	}
	lanes := hwy.Zero[float32]().NumLanes()
	var i int
	for i = 0; i+1 < blockDim; i += 2 {
		cRow0Start := i * blockDim
		cRow1Start := (i + 1) * blockDim
		for k := 0; k < blockDim; k++ {
			a0k := aT[k*blockDim+i]
			a1k := aT[k*blockDim+i+1]
			vA0 := hwy.Set(a0k)
			vA1 := hwy.Set(a1k)
			bRowStart := k * blockDim
			var j int
			for j = 0; j+lanes <= blockDim; j += lanes {
				vB := hwy.Load(b[bRowStart+j:])
				vC0 := hwy.Load(c[cRow0Start+j:])
				vC0 = hwy.MulAdd(vA0, vB, vC0)
				hwy.Store(vC0, c[cRow0Start+j:])
				vC1 := hwy.Load(c[cRow1Start+j:])
				vC1 = hwy.MulAdd(vA1, vB, vC1)
				hwy.Store(vC1, c[cRow1Start+j:])
			}
			for ; j < blockDim; j++ {
				c[cRow0Start+j] += a0k * b[bRowStart+j]
				c[cRow1Start+j] += a1k * b[bRowStart+j]
			}
		}
	}
	if i < blockDim {
		cRowStart := i * blockDim
		for k := 0; k < blockDim; k++ {
			aik := aT[k*blockDim+i]
			vA := hwy.Set(aik)
			bRowStart := k * blockDim
			var j int
			for j = 0; j+lanes <= blockDim; j += lanes {
				vB := hwy.Load(b[bRowStart+j:])
				vC := hwy.Load(c[cRowStart+j:])
				vC = hwy.MulAdd(vA, vB, vC)
				hwy.Store(vC, c[cRowStart+j:])
			}
			for ; j < blockDim; j++ {
				c[cRowStart+j] += aik * b[bRowStart+j]
			}
		}
	}
}

func BaseBlockMulAdd2_fallback_Float64(aT []float64, b []float64, c []float64, blockDim int) {
	if len(aT) < blockDim*blockDim {
		panic("BlockMulAdd2: aT slice too short")
	}
	if len(b) < blockDim*blockDim {
		panic("BlockMulAdd2: B slice too short")
	}
	if len(c) < blockDim*blockDim {
		panic("BlockMulAdd2: C slice too short")
	}
	lanes := hwy.Zero[float64]().NumLanes()
	var i int
	for i = 0; i+1 < blockDim; i += 2 {
		cRow0Start := i * blockDim
		cRow1Start := (i + 1) * blockDim
		for k := 0; k < blockDim; k++ {
			a0k := aT[k*blockDim+i]
			a1k := aT[k*blockDim+i+1]
			vA0 := hwy.Set(a0k)
			vA1 := hwy.Set(a1k)
			bRowStart := k * blockDim
			var j int
			for j = 0; j+lanes <= blockDim; j += lanes {
				vB := hwy.Load(b[bRowStart+j:])
				vC0 := hwy.Load(c[cRow0Start+j:])
				vC0 = hwy.MulAdd(vA0, vB, vC0)
				hwy.Store(vC0, c[cRow0Start+j:])
				vC1 := hwy.Load(c[cRow1Start+j:])
				vC1 = hwy.MulAdd(vA1, vB, vC1)
				hwy.Store(vC1, c[cRow1Start+j:])
			}
			for ; j < blockDim; j++ {
				c[cRow0Start+j] += a0k * b[bRowStart+j]
				c[cRow1Start+j] += a1k * b[bRowStart+j]
			}
		}
	}
	if i < blockDim {
		cRowStart := i * blockDim
		for k := 0; k < blockDim; k++ {
			aik := aT[k*blockDim+i]
			vA := hwy.Set(aik)
			bRowStart := k * blockDim
			var j int
			for j = 0; j+lanes <= blockDim; j += lanes {
				vB := hwy.Load(b[bRowStart+j:])
				vC := hwy.Load(c[cRowStart+j:])
				vC = hwy.MulAdd(vA, vB, vC)
				hwy.Store(vC, c[cRowStart+j:])
			}
			for ; j < blockDim; j++ {
				c[cRowStart+j] += aik * b[bRowStart+j]
			}
		}
	}
}

func BaseBlockMulAdd4_fallback(aT []float32, b []float32, c []float32, blockDim int) {
	if len(aT) < blockDim*blockDim {
		panic("BlockMulAdd4: aT slice too short")
	}
	if len(b) < blockDim*blockDim {
		panic("BlockMulAdd4: B slice too short")
	}
	if len(c) < blockDim*blockDim {
		panic("BlockMulAdd4: C slice too short")
	}
	lanes := hwy.Zero[float32]().NumLanes()
	var i int
	for i = 0; i+3 < blockDim; i += 4 {
		cRow0 := i * blockDim
		cRow1 := (i + 1) * blockDim
		cRow2 := (i + 2) * blockDim
		cRow3 := (i + 3) * blockDim
		for k := 0; k < blockDim; k++ {
			aTRowK := k * blockDim
			a0k := aT[aTRowK+i]
			a1k := aT[aTRowK+i+1]
			a2k := aT[aTRowK+i+2]
			a3k := aT[aTRowK+i+3]
			vA0 := hwy.Set(a0k)
			vA1 := hwy.Set(a1k)
			vA2 := hwy.Set(a2k)
			vA3 := hwy.Set(a3k)
			bRowStart := k * blockDim
			var j int
			for j = 0; j+lanes <= blockDim; j += lanes {
				vB := hwy.Load(b[bRowStart+j:])
				vC0 := hwy.Load(c[cRow0+j:])
				vC0 = hwy.MulAdd(vA0, vB, vC0)
				hwy.Store(vC0, c[cRow0+j:])
				vC1 := hwy.Load(c[cRow1+j:])
				vC1 = hwy.MulAdd(vA1, vB, vC1)
				hwy.Store(vC1, c[cRow1+j:])
				vC2 := hwy.Load(c[cRow2+j:])
				vC2 = hwy.MulAdd(vA2, vB, vC2)
				hwy.Store(vC2, c[cRow2+j:])
				vC3 := hwy.Load(c[cRow3+j:])
				vC3 = hwy.MulAdd(vA3, vB, vC3)
				hwy.Store(vC3, c[cRow3+j:])
			}
			for ; j < blockDim; j++ {
				c[cRow0+j] += a0k * b[bRowStart+j]
				c[cRow1+j] += a1k * b[bRowStart+j]
				c[cRow2+j] += a2k * b[bRowStart+j]
				c[cRow3+j] += a3k * b[bRowStart+j]
			}
		}
	}
	for ; i < blockDim; i++ {
		cRowStart := i * blockDim
		for k := 0; k < blockDim; k++ {
			aik := aT[k*blockDim+i]
			vA := hwy.Set(aik)
			bRowStart := k * blockDim
			var j int
			for j = 0; j+lanes <= blockDim; j += lanes {
				vB := hwy.Load(b[bRowStart+j:])
				vC := hwy.Load(c[cRowStart+j:])
				vC = hwy.MulAdd(vA, vB, vC)
				hwy.Store(vC, c[cRowStart+j:])
			}
			for ; j < blockDim; j++ {
				c[cRowStart+j] += aik * b[bRowStart+j]
			}
		}
	}
}

func BaseBlockMulAdd4_fallback_Float64(aT []float64, b []float64, c []float64, blockDim int) {
	if len(aT) < blockDim*blockDim {
		panic("BlockMulAdd4: aT slice too short")
	}
	if len(b) < blockDim*blockDim {
		panic("BlockMulAdd4: B slice too short")
	}
	if len(c) < blockDim*blockDim {
		panic("BlockMulAdd4: C slice too short")
	}
	lanes := hwy.Zero[float64]().NumLanes()
	var i int
	for i = 0; i+3 < blockDim; i += 4 {
		cRow0 := i * blockDim
		cRow1 := (i + 1) * blockDim
		cRow2 := (i + 2) * blockDim
		cRow3 := (i + 3) * blockDim
		for k := 0; k < blockDim; k++ {
			aTRowK := k * blockDim
			a0k := aT[aTRowK+i]
			a1k := aT[aTRowK+i+1]
			a2k := aT[aTRowK+i+2]
			a3k := aT[aTRowK+i+3]
			vA0 := hwy.Set(a0k)
			vA1 := hwy.Set(a1k)
			vA2 := hwy.Set(a2k)
			vA3 := hwy.Set(a3k)
			bRowStart := k * blockDim
			var j int
			for j = 0; j+lanes <= blockDim; j += lanes {
				vB := hwy.Load(b[bRowStart+j:])
				vC0 := hwy.Load(c[cRow0+j:])
				vC0 = hwy.MulAdd(vA0, vB, vC0)
				hwy.Store(vC0, c[cRow0+j:])
				vC1 := hwy.Load(c[cRow1+j:])
				vC1 = hwy.MulAdd(vA1, vB, vC1)
				hwy.Store(vC1, c[cRow1+j:])
				vC2 := hwy.Load(c[cRow2+j:])
				vC2 = hwy.MulAdd(vA2, vB, vC2)
				hwy.Store(vC2, c[cRow2+j:])
				vC3 := hwy.Load(c[cRow3+j:])
				vC3 = hwy.MulAdd(vA3, vB, vC3)
				hwy.Store(vC3, c[cRow3+j:])
			}
			for ; j < blockDim; j++ {
				c[cRow0+j] += a0k * b[bRowStart+j]
				c[cRow1+j] += a1k * b[bRowStart+j]
				c[cRow2+j] += a2k * b[bRowStart+j]
				c[cRow3+j] += a3k * b[bRowStart+j]
			}
		}
	}
	for ; i < blockDim; i++ {
		cRowStart := i * blockDim
		for k := 0; k < blockDim; k++ {
			aik := aT[k*blockDim+i]
			vA := hwy.Set(aik)
			bRowStart := k * blockDim
			var j int
			for j = 0; j+lanes <= blockDim; j += lanes {
				vB := hwy.Load(b[bRowStart+j:])
				vC := hwy.Load(c[cRowStart+j:])
				vC = hwy.MulAdd(vA, vB, vC)
				hwy.Store(vC, c[cRowStart+j:])
			}
			for ; j < blockDim; j++ {
				c[cRowStart+j] += aik * b[bRowStart+j]
			}
		}
	}
}
