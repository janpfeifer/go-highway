// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

//go:build arm64

package matmul

import (
	stdmath "math"
	"unsafe"

	"github.com/ajroetker/go-highway/hwy/asm"
	"github.com/ajroetker/go-highway/hwy/contrib/math"
)

// Hoisted constants - pre-broadcasted at package init time
var (
	BaseFusedInt4MatMulGELUApprox_NEON_coeff_f32 = asm.BroadcastFloat32x4(float32(1.702))
	BaseFusedInt4MatMulGELU_NEON_half_f32        = asm.BroadcastFloat32x4(float32(0.5))
	BaseFusedInt4MatMulGELU_NEON_invSqrt2_f32    = asm.BroadcastFloat32x4(float32(0.7071067811865476))
	BaseFusedInt4MatMulGELU_NEON_one_f32         = asm.BroadcastFloat32x4(float32(1.0))
	BaseFusedNF4MatMulGELUApprox_NEON_coeff_f32  = asm.BroadcastFloat32x4(float32(1.702))
	BaseFusedNF4MatMulGELU_NEON_half_f32         = asm.BroadcastFloat32x4(float32(0.5))
	BaseFusedNF4MatMulGELU_NEON_invSqrt2_f32     = asm.BroadcastFloat32x4(float32(0.7071067811865476))
	BaseFusedNF4MatMulGELU_NEON_one_f32          = asm.BroadcastFloat32x4(float32(1.0))
)

func BaseFusedNF4MatMulSiLU_neon(input []float32, packed []uint8, scales []float32, output []float32, M int, K int, N int, groupSize int) {
	if M == 0 || K == 0 || N == 0 {
		return
	}
	numGroups := (N + groupSize - 1) / groupSize
	lanes := 4
	dequantBuf := [4]float32{}
	for m := 0; m < M; m++ {
		inputRow := input[m*K : (m+1)*K]
		outputRow := output[m*N : (m+1)*N]
		var n int
		for n = 0; n+lanes <= N; n += lanes {
			acc := asm.ZeroFloat32x4()
			for k := 0; k < K; k++ {
				inputVal := asm.BroadcastFloat32x4(inputRow[k])
				baseIdx := k * N
				scaleBase := k * numGroups
				for lane := 0; lane < lanes; lane++ {
					colIdx := n + lane
					weightIdx := baseIdx + colIdx
					packedIdx := weightIdx / 2
					var quantIdx int
					if weightIdx%2 == 0 {
						quantIdx = int(packed[packedIdx] & 0x0F)
					} else {
						quantIdx = int((packed[packedIdx] >> 4) & 0x0F)
					}
					groupIdx := colIdx / groupSize
					scale := scales[scaleBase+groupIdx]
					dequantBuf[lane] = nf4LookupTable[quantIdx] * scale
				}
				weights := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&dequantBuf[0])))
				inputVal.MulAddAcc(weights, &acc)
			}
			sig := math.BaseSigmoidVec_neon(acc)
			acc = acc.Mul(sig)
			acc.Store((*[4]float32)(unsafe.Pointer(&outputRow[n])))
		}
		for ; n < N; n++ {
			groupIdx := n / groupSize
			sum := float32(0)
			for k := 0; k < K; k++ {
				weightIdx := k*N + n
				packedIdx := weightIdx / 2
				var quantIdx int
				if weightIdx%2 == 0 {
					quantIdx = int(packed[packedIdx] & 0x0F)
				} else {
					quantIdx = int((packed[packedIdx] >> 4) & 0x0F)
				}
				scale := scales[k*numGroups+groupIdx]
				weight := nf4LookupTable[quantIdx] * scale
				sum += inputRow[k] * weight
			}
			outputRow[n] = sum / (1.0 + float32(stdmath.Exp(float64(-sum))))
		}
	}
}

func BaseFusedNF4MatMulGELU_neon(input []float32, packed []uint8, scales []float32, output []float32, M int, K int, N int, groupSize int) {
	if M == 0 || K == 0 || N == 0 {
		return
	}
	numGroups := (N + groupSize - 1) / groupSize
	lanes := 4
	dequantBuf := [4]float32{}
	for m := 0; m < M; m++ {
		inputRow := input[m*K : (m+1)*K]
		outputRow := output[m*N : (m+1)*N]
		var n int
		for n = 0; n+lanes <= N; n += lanes {
			acc := asm.ZeroFloat32x4()
			for k := 0; k < K; k++ {
				inputVal := asm.BroadcastFloat32x4(inputRow[k])
				baseIdx := k * N
				scaleBase := k * numGroups
				for lane := 0; lane < lanes; lane++ {
					colIdx := n + lane
					weightIdx := baseIdx + colIdx
					packedIdx := weightIdx / 2
					var quantIdx int
					if weightIdx%2 == 0 {
						quantIdx = int(packed[packedIdx] & 0x0F)
					} else {
						quantIdx = int((packed[packedIdx] >> 4) & 0x0F)
					}
					groupIdx := colIdx / groupSize
					scale := scales[scaleBase+groupIdx]
					dequantBuf[lane] = nf4LookupTable[quantIdx] * scale
				}
				weights := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&dequantBuf[0])))
				inputVal.MulAddAcc(weights, &acc)
			}
			invSqrt2 := BaseFusedNF4MatMulGELU_NEON_invSqrt2_f32
			half := BaseFusedNF4MatMulGELU_NEON_half_f32
			one := BaseFusedNF4MatMulGELU_NEON_one_f32
			scaled := acc.Mul(invSqrt2)
			erfVal := math.BaseErfVec_neon(scaled)
			acc = acc.Mul(half.Mul(one.Add(erfVal)))
			acc.Store((*[4]float32)(unsafe.Pointer(&outputRow[n])))
		}
		for ; n < N; n++ {
			groupIdx := n / groupSize
			sum := float32(0)
			for k := 0; k < K; k++ {
				weightIdx := k*N + n
				packedIdx := weightIdx / 2
				var quantIdx int
				if weightIdx%2 == 0 {
					quantIdx = int(packed[packedIdx] & 0x0F)
				} else {
					quantIdx = int((packed[packedIdx] >> 4) & 0x0F)
				}
				scale := scales[k*numGroups+groupIdx]
				weight := nf4LookupTable[quantIdx] * scale
				sum += inputRow[k] * weight
			}
			outputRow[n] = sum * 0.5 * (1.0 + float32(stdmath.Erf(float64(sum)*0.7071067811865476)))
		}
	}
}

func BaseFusedNF4MatMulGELUApprox_neon(input []float32, packed []uint8, scales []float32, output []float32, M int, K int, N int, groupSize int) {
	if M == 0 || K == 0 || N == 0 {
		return
	}
	numGroups := (N + groupSize - 1) / groupSize
	lanes := 4
	dequantBuf := [4]float32{}
	for m := 0; m < M; m++ {
		inputRow := input[m*K : (m+1)*K]
		outputRow := output[m*N : (m+1)*N]
		var n int
		for n = 0; n+lanes <= N; n += lanes {
			acc := asm.ZeroFloat32x4()
			for k := 0; k < K; k++ {
				inputVal := asm.BroadcastFloat32x4(inputRow[k])
				baseIdx := k * N
				scaleBase := k * numGroups
				for lane := 0; lane < lanes; lane++ {
					colIdx := n + lane
					weightIdx := baseIdx + colIdx
					packedIdx := weightIdx / 2
					var quantIdx int
					if weightIdx%2 == 0 {
						quantIdx = int(packed[packedIdx] & 0x0F)
					} else {
						quantIdx = int((packed[packedIdx] >> 4) & 0x0F)
					}
					groupIdx := colIdx / groupSize
					scale := scales[scaleBase+groupIdx]
					dequantBuf[lane] = nf4LookupTable[quantIdx] * scale
				}
				weights := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&dequantBuf[0])))
				inputVal.MulAddAcc(weights, &acc)
			}
			coeff := BaseFusedNF4MatMulGELUApprox_NEON_coeff_f32
			scaled := acc.Mul(coeff)
			sig := math.BaseSigmoidVec_neon(scaled)
			acc = acc.Mul(sig)
			acc.Store((*[4]float32)(unsafe.Pointer(&outputRow[n])))
		}
		for ; n < N; n++ {
			groupIdx := n / groupSize
			sum := float32(0)
			for k := 0; k < K; k++ {
				weightIdx := k*N + n
				packedIdx := weightIdx / 2
				var quantIdx int
				if weightIdx%2 == 0 {
					quantIdx = int(packed[packedIdx] & 0x0F)
				} else {
					quantIdx = int((packed[packedIdx] >> 4) & 0x0F)
				}
				scale := scales[k*numGroups+groupIdx]
				weight := nf4LookupTable[quantIdx] * scale
				sum += inputRow[k] * weight
			}
			outputRow[n] = sum / (1.0 + float32(stdmath.Exp(float64(-1.702*sum))))
		}
	}
}

func BaseFusedNF4MatMulReLU_neon(input []float32, packed []uint8, scales []float32, output []float32, M int, K int, N int, groupSize int) {
	if M == 0 || K == 0 || N == 0 {
		return
	}
	numGroups := (N + groupSize - 1) / groupSize
	lanes := 4
	dequantBuf := [4]float32{}
	for m := 0; m < M; m++ {
		inputRow := input[m*K : (m+1)*K]
		outputRow := output[m*N : (m+1)*N]
		var n int
		for n = 0; n+lanes <= N; n += lanes {
			acc := asm.ZeroFloat32x4()
			for k := 0; k < K; k++ {
				inputVal := asm.BroadcastFloat32x4(inputRow[k])
				baseIdx := k * N
				scaleBase := k * numGroups
				for lane := 0; lane < lanes; lane++ {
					colIdx := n + lane
					weightIdx := baseIdx + colIdx
					packedIdx := weightIdx / 2
					var quantIdx int
					if weightIdx%2 == 0 {
						quantIdx = int(packed[packedIdx] & 0x0F)
					} else {
						quantIdx = int((packed[packedIdx] >> 4) & 0x0F)
					}
					groupIdx := colIdx / groupSize
					scale := scales[scaleBase+groupIdx]
					dequantBuf[lane] = nf4LookupTable[quantIdx] * scale
				}
				weights := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&dequantBuf[0])))
				inputVal.MulAddAcc(weights, &acc)
			}
			acc = acc.Max(asm.ZeroFloat32x4())
			acc.Store((*[4]float32)(unsafe.Pointer(&outputRow[n])))
		}
		for ; n < N; n++ {
			groupIdx := n / groupSize
			sum := float32(0)
			for k := 0; k < K; k++ {
				weightIdx := k*N + n
				packedIdx := weightIdx / 2
				var quantIdx int
				if weightIdx%2 == 0 {
					quantIdx = int(packed[packedIdx] & 0x0F)
				} else {
					quantIdx = int((packed[packedIdx] >> 4) & 0x0F)
				}
				scale := scales[k*numGroups+groupIdx]
				weight := nf4LookupTable[quantIdx] * scale
				sum += inputRow[k] * weight
			}
			outputRow[n] = float32(stdmath.Max(0, float64(sum)))
		}
	}
}

func BaseFusedInt4MatMulSiLU_neon(input []float32, packed []uint8, scales []float32, output []float32, M int, K int, N int, groupSize int) {
	if M == 0 || K == 0 || N == 0 {
		return
	}
	numGroups := (N + groupSize - 1) / groupSize
	lanes := 4
	dequantBuf := [4]float32{}
	for m := 0; m < M; m++ {
		inputRow := input[m*K : (m+1)*K]
		outputRow := output[m*N : (m+1)*N]
		var n int
		for n = 0; n+lanes <= N; n += lanes {
			acc := asm.ZeroFloat32x4()
			for k := 0; k < K; k++ {
				inputVal := asm.BroadcastFloat32x4(inputRow[k])
				baseIdx := k * N
				scaleBase := k * numGroups
				for lane := 0; lane < lanes; lane++ {
					colIdx := n + lane
					weightIdx := baseIdx + colIdx
					packedIdx := weightIdx / 2
					var unsignedVal int
					if weightIdx%2 == 0 {
						unsignedVal = int(packed[packedIdx] & 0x0F)
					} else {
						unsignedVal = int((packed[packedIdx] >> 4) & 0x0F)
					}
					groupIdx := colIdx / groupSize
					scale := scales[scaleBase+groupIdx]
					dequantBuf[lane] = float32(unsignedVal-8) * scale
				}
				weights := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&dequantBuf[0])))
				inputVal.MulAddAcc(weights, &acc)
			}
			sig := math.BaseSigmoidVec_neon(acc)
			acc = acc.Mul(sig)
			acc.Store((*[4]float32)(unsafe.Pointer(&outputRow[n])))
		}
		for ; n < N; n++ {
			groupIdx := n / groupSize
			sum := float32(0)
			for k := 0; k < K; k++ {
				weightIdx := k*N + n
				packedIdx := weightIdx / 2
				var unsignedVal int
				if weightIdx%2 == 0 {
					unsignedVal = int(packed[packedIdx] & 0x0F)
				} else {
					unsignedVal = int((packed[packedIdx] >> 4) & 0x0F)
				}
				scale := scales[k*numGroups+groupIdx]
				weight := float32(unsignedVal-8) * scale
				sum += inputRow[k] * weight
			}
			outputRow[n] = sum / (1.0 + float32(stdmath.Exp(float64(-sum))))
		}
	}
}

func BaseFusedInt4MatMulGELU_neon(input []float32, packed []uint8, scales []float32, output []float32, M int, K int, N int, groupSize int) {
	if M == 0 || K == 0 || N == 0 {
		return
	}
	numGroups := (N + groupSize - 1) / groupSize
	lanes := 4
	dequantBuf := [4]float32{}
	for m := 0; m < M; m++ {
		inputRow := input[m*K : (m+1)*K]
		outputRow := output[m*N : (m+1)*N]
		var n int
		for n = 0; n+lanes <= N; n += lanes {
			acc := asm.ZeroFloat32x4()
			for k := 0; k < K; k++ {
				inputVal := asm.BroadcastFloat32x4(inputRow[k])
				baseIdx := k * N
				scaleBase := k * numGroups
				for lane := 0; lane < lanes; lane++ {
					colIdx := n + lane
					weightIdx := baseIdx + colIdx
					packedIdx := weightIdx / 2
					var unsignedVal int
					if weightIdx%2 == 0 {
						unsignedVal = int(packed[packedIdx] & 0x0F)
					} else {
						unsignedVal = int((packed[packedIdx] >> 4) & 0x0F)
					}
					groupIdx := colIdx / groupSize
					scale := scales[scaleBase+groupIdx]
					dequantBuf[lane] = float32(unsignedVal-8) * scale
				}
				weights := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&dequantBuf[0])))
				inputVal.MulAddAcc(weights, &acc)
			}
			invSqrt2 := BaseFusedInt4MatMulGELU_NEON_invSqrt2_f32
			half := BaseFusedInt4MatMulGELU_NEON_half_f32
			one := BaseFusedInt4MatMulGELU_NEON_one_f32
			scaled := acc.Mul(invSqrt2)
			erfVal := math.BaseErfVec_neon(scaled)
			acc = acc.Mul(half.Mul(one.Add(erfVal)))
			acc.Store((*[4]float32)(unsafe.Pointer(&outputRow[n])))
		}
		for ; n < N; n++ {
			groupIdx := n / groupSize
			sum := float32(0)
			for k := 0; k < K; k++ {
				weightIdx := k*N + n
				packedIdx := weightIdx / 2
				var unsignedVal int
				if weightIdx%2 == 0 {
					unsignedVal = int(packed[packedIdx] & 0x0F)
				} else {
					unsignedVal = int((packed[packedIdx] >> 4) & 0x0F)
				}
				scale := scales[k*numGroups+groupIdx]
				weight := float32(unsignedVal-8) * scale
				sum += inputRow[k] * weight
			}
			outputRow[n] = sum * 0.5 * (1.0 + float32(stdmath.Erf(float64(sum)*0.7071067811865476)))
		}
	}
}

func BaseFusedInt4MatMulGELUApprox_neon(input []float32, packed []uint8, scales []float32, output []float32, M int, K int, N int, groupSize int) {
	if M == 0 || K == 0 || N == 0 {
		return
	}
	numGroups := (N + groupSize - 1) / groupSize
	lanes := 4
	dequantBuf := [4]float32{}
	for m := 0; m < M; m++ {
		inputRow := input[m*K : (m+1)*K]
		outputRow := output[m*N : (m+1)*N]
		var n int
		for n = 0; n+lanes <= N; n += lanes {
			acc := asm.ZeroFloat32x4()
			for k := 0; k < K; k++ {
				inputVal := asm.BroadcastFloat32x4(inputRow[k])
				baseIdx := k * N
				scaleBase := k * numGroups
				for lane := 0; lane < lanes; lane++ {
					colIdx := n + lane
					weightIdx := baseIdx + colIdx
					packedIdx := weightIdx / 2
					var unsignedVal int
					if weightIdx%2 == 0 {
						unsignedVal = int(packed[packedIdx] & 0x0F)
					} else {
						unsignedVal = int((packed[packedIdx] >> 4) & 0x0F)
					}
					groupIdx := colIdx / groupSize
					scale := scales[scaleBase+groupIdx]
					dequantBuf[lane] = float32(unsignedVal-8) * scale
				}
				weights := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&dequantBuf[0])))
				inputVal.MulAddAcc(weights, &acc)
			}
			coeff := BaseFusedInt4MatMulGELUApprox_NEON_coeff_f32
			scaled := acc.Mul(coeff)
			sig := math.BaseSigmoidVec_neon(scaled)
			acc = acc.Mul(sig)
			acc.Store((*[4]float32)(unsafe.Pointer(&outputRow[n])))
		}
		for ; n < N; n++ {
			groupIdx := n / groupSize
			sum := float32(0)
			for k := 0; k < K; k++ {
				weightIdx := k*N + n
				packedIdx := weightIdx / 2
				var unsignedVal int
				if weightIdx%2 == 0 {
					unsignedVal = int(packed[packedIdx] & 0x0F)
				} else {
					unsignedVal = int((packed[packedIdx] >> 4) & 0x0F)
				}
				scale := scales[k*numGroups+groupIdx]
				weight := float32(unsignedVal-8) * scale
				sum += inputRow[k] * weight
			}
			outputRow[n] = sum / (1.0 + float32(stdmath.Exp(float64(-1.702*sum))))
		}
	}
}

func BaseFusedInt4MatMulReLU_neon(input []float32, packed []uint8, scales []float32, output []float32, M int, K int, N int, groupSize int) {
	if M == 0 || K == 0 || N == 0 {
		return
	}
	numGroups := (N + groupSize - 1) / groupSize
	lanes := 4
	dequantBuf := [4]float32{}
	for m := 0; m < M; m++ {
		inputRow := input[m*K : (m+1)*K]
		outputRow := output[m*N : (m+1)*N]
		var n int
		for n = 0; n+lanes <= N; n += lanes {
			acc := asm.ZeroFloat32x4()
			for k := 0; k < K; k++ {
				inputVal := asm.BroadcastFloat32x4(inputRow[k])
				baseIdx := k * N
				scaleBase := k * numGroups
				for lane := 0; lane < lanes; lane++ {
					colIdx := n + lane
					weightIdx := baseIdx + colIdx
					packedIdx := weightIdx / 2
					var unsignedVal int
					if weightIdx%2 == 0 {
						unsignedVal = int(packed[packedIdx] & 0x0F)
					} else {
						unsignedVal = int((packed[packedIdx] >> 4) & 0x0F)
					}
					groupIdx := colIdx / groupSize
					scale := scales[scaleBase+groupIdx]
					dequantBuf[lane] = float32(unsignedVal-8) * scale
				}
				weights := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&dequantBuf[0])))
				inputVal.MulAddAcc(weights, &acc)
			}
			acc = acc.Max(asm.ZeroFloat32x4())
			acc.Store((*[4]float32)(unsafe.Pointer(&outputRow[n])))
		}
		for ; n < N; n++ {
			groupIdx := n / groupSize
			sum := float32(0)
			for k := 0; k < K; k++ {
				weightIdx := k*N + n
				packedIdx := weightIdx / 2
				var unsignedVal int
				if weightIdx%2 == 0 {
					unsignedVal = int(packed[packedIdx] & 0x0F)
				} else {
					unsignedVal = int((packed[packedIdx] >> 4) & 0x0F)
				}
				scale := scales[k*numGroups+groupIdx]
				weight := float32(unsignedVal-8) * scale
				sum += inputRow[k] * weight
			}
			outputRow[n] = float32(stdmath.Max(0, float64(sum)))
		}
	}
}

func BaseFusedNF4MatMulSwiGLU_neon(input []float32, gatePacked []uint8, gateScales []float32, upPacked []uint8, upScales []float32, output []float32, M int, K int, N int, groupSize int) {
	if M == 0 || K == 0 || N == 0 {
		return
	}
	numGroups := (N + groupSize - 1) / groupSize
	lanes := 4
	gateBuf := [4]float32{}
	upBuf := [4]float32{}
	for m := 0; m < M; m++ {
		inputRow := input[m*K : (m+1)*K]
		outputRow := output[m*N : (m+1)*N]
		var n int
		for n = 0; n+lanes <= N; n += lanes {
			gateAcc := asm.ZeroFloat32x4()
			upAcc := asm.ZeroFloat32x4()
			for k := 0; k < K; k++ {
				inputVal := asm.BroadcastFloat32x4(inputRow[k])
				baseIdx := k * N
				scaleBase := k * numGroups
				for lane := 0; lane < lanes; lane++ {
					colIdx := n + lane
					weightIdx := baseIdx + colIdx
					packedIdx := weightIdx / 2
					groupIdx := colIdx / groupSize
					var gateQuantIdx int
					if weightIdx%2 == 0 {
						gateQuantIdx = int(gatePacked[packedIdx] & 0x0F)
					} else {
						gateQuantIdx = int((gatePacked[packedIdx] >> 4) & 0x0F)
					}
					gateScale := gateScales[scaleBase+groupIdx]
					gateBuf[lane] = nf4LookupTable[gateQuantIdx] * gateScale
					var upQuantIdx int
					if weightIdx%2 == 0 {
						upQuantIdx = int(upPacked[packedIdx] & 0x0F)
					} else {
						upQuantIdx = int((upPacked[packedIdx] >> 4) & 0x0F)
					}
					upScale := upScales[scaleBase+groupIdx]
					upBuf[lane] = nf4LookupTable[upQuantIdx] * upScale
				}
				gateWeights := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&gateBuf[0])))
				upWeights := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&upBuf[0])))
				inputVal.MulAddAcc(gateWeights, &gateAcc)
				inputVal.MulAddAcc(upWeights, &upAcc)
			}
			gateSilu := gateAcc.Mul(math.BaseSigmoidVec_neon(gateAcc))
			result := gateSilu.Mul(upAcc)
			result.Store((*[4]float32)(unsafe.Pointer(&outputRow[n])))
		}
		for ; n < N; n++ {
			groupIdx := n / groupSize
			gateSum := float32(0)
			upSum := float32(0)
			for k := 0; k < K; k++ {
				weightIdx := k*N + n
				packedIdx := weightIdx / 2
				var gateQuantIdx int
				if weightIdx%2 == 0 {
					gateQuantIdx = int(gatePacked[packedIdx] & 0x0F)
				} else {
					gateQuantIdx = int((gatePacked[packedIdx] >> 4) & 0x0F)
				}
				gateScale := gateScales[k*numGroups+groupIdx]
				gateSum += inputRow[k] * nf4LookupTable[gateQuantIdx] * gateScale
				var upQuantIdx int
				if weightIdx%2 == 0 {
					upQuantIdx = int(upPacked[packedIdx] & 0x0F)
				} else {
					upQuantIdx = int((upPacked[packedIdx] >> 4) & 0x0F)
				}
				upScale := upScales[k*numGroups+groupIdx]
				upSum += inputRow[k] * nf4LookupTable[upQuantIdx] * upScale
			}
			gateSilu := gateSum / (1.0 + float32(stdmath.Exp(float64(-gateSum))))
			outputRow[n] = gateSilu * upSum
		}
	}
}

func BaseFusedInt4MatMulSwiGLU_neon(input []float32, gatePacked []uint8, gateScales []float32, upPacked []uint8, upScales []float32, output []float32, M int, K int, N int, groupSize int) {
	if M == 0 || K == 0 || N == 0 {
		return
	}
	numGroups := (N + groupSize - 1) / groupSize
	lanes := 4
	gateBuf := [4]float32{}
	upBuf := [4]float32{}
	for m := 0; m < M; m++ {
		inputRow := input[m*K : (m+1)*K]
		outputRow := output[m*N : (m+1)*N]
		var n int
		for n = 0; n+lanes <= N; n += lanes {
			gateAcc := asm.ZeroFloat32x4()
			upAcc := asm.ZeroFloat32x4()
			for k := 0; k < K; k++ {
				inputVal := asm.BroadcastFloat32x4(inputRow[k])
				baseIdx := k * N
				scaleBase := k * numGroups
				for lane := 0; lane < lanes; lane++ {
					colIdx := n + lane
					weightIdx := baseIdx + colIdx
					packedIdx := weightIdx / 2
					groupIdx := colIdx / groupSize
					var gateUnsigned int
					if weightIdx%2 == 0 {
						gateUnsigned = int(gatePacked[packedIdx] & 0x0F)
					} else {
						gateUnsigned = int((gatePacked[packedIdx] >> 4) & 0x0F)
					}
					gateScale := gateScales[scaleBase+groupIdx]
					gateBuf[lane] = float32(gateUnsigned-8) * gateScale
					var upUnsigned int
					if weightIdx%2 == 0 {
						upUnsigned = int(upPacked[packedIdx] & 0x0F)
					} else {
						upUnsigned = int((upPacked[packedIdx] >> 4) & 0x0F)
					}
					upScale := upScales[scaleBase+groupIdx]
					upBuf[lane] = float32(upUnsigned-8) * upScale
				}
				gateWeights := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&gateBuf[0])))
				upWeights := asm.LoadFloat32x4((*[4]float32)(unsafe.Pointer(&upBuf[0])))
				inputVal.MulAddAcc(gateWeights, &gateAcc)
				inputVal.MulAddAcc(upWeights, &upAcc)
			}
			gateSilu := gateAcc.Mul(math.BaseSigmoidVec_neon(gateAcc))
			result := gateSilu.Mul(upAcc)
			result.Store((*[4]float32)(unsafe.Pointer(&outputRow[n])))
		}
		for ; n < N; n++ {
			groupIdx := n / groupSize
			gateSum := float32(0)
			upSum := float32(0)
			for k := 0; k < K; k++ {
				weightIdx := k*N + n
				packedIdx := weightIdx / 2
				var gateUnsigned int
				if weightIdx%2 == 0 {
					gateUnsigned = int(gatePacked[packedIdx] & 0x0F)
				} else {
					gateUnsigned = int((gatePacked[packedIdx] >> 4) & 0x0F)
				}
				gateScale := gateScales[k*numGroups+groupIdx]
				gateSum += inputRow[k] * float32(gateUnsigned-8) * gateScale
				var upUnsigned int
				if weightIdx%2 == 0 {
					upUnsigned = int(upPacked[packedIdx] & 0x0F)
				} else {
					upUnsigned = int((upPacked[packedIdx] >> 4) & 0x0F)
				}
				upScale := upScales[k*numGroups+groupIdx]
				upSum += inputRow[k] * float32(upUnsigned-8) * upScale
			}
			gateSilu := gateSum / (1.0 + float32(stdmath.Exp(float64(-gateSum))))
			outputRow[n] = gateSilu * upSum
		}
	}
}
